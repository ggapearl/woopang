# FilterButtonPanel 빠른 설정 체크리스트

## ✅ 현재 완료 상태
- [x] FilterManager.cs 코드 수정 (길게 누르기 기능)
- [x] DataManager.cs 디버깅 로그 추가
- [x] FilterButtonPanel.prefab 기본 구조 생성
- [x] 토글 크기 2배 증가 (60x60)
- [x] 왼쪽 상단 정렬
- [x] VerticalLayoutGroup 간격 축소 (5px)

## ⚠️ Unity Editor에서 해야 할 작업

### 📝 단계별 작업 (15분 소요)

#### 1. 프리팹 편집 모드 진입
```
1. Project 창에서 Assets/Prefabs/FilterButtonPanel.prefab 더블클릭
2. Prefab Mode로 진입 확인 (상단 "<" 버튼이 보임)
```

#### 2. 토글 4개 추가 (5분)
```
PetFriendlyToggle 선택 → Ctrl+D (복제)
├─ 이름: SubwayToggle
│  └─ 자식 Label > Text: "지하철" 로 변경
│
├─ 이름: BusToggle
│  └─ 자식 Label > Text: "버스" 로 변경
│
├─ 이름: AlcoholToggle
│  └─ 자식 Label > Text: "주류판매" 로 변경
│
└─ 이름: WoopangDataToggle
   └─ 자식 Label > Text: "우팡데이터" 로 변경
```

#### 3. 버튼 2개 추가 (3분)
```
FilterButtonPanel 우클릭 > UI > Button

첫 번째 버튼:
├─ 이름: SelectAllButton
├─ RectTransform > Height: 50
├─ Image > Color: 파란색 (R:51, G:153, B:255, A:204)
└─ 자식 Text > Text: "전체 선택"

두 번째 버튼:
├─ 이름: DeselectAllButton
├─ RectTransform > Height: 50
├─ Image > Color: 회색 (R:200, G:200, B:200, A:204)
└─ 자식 Text > Text: "전체 해제"
```

#### 4. FilterManager 연결 (2분)
```
FilterButtonPanel 선택 > Inspector > FilterManager 컴포넌트

드래그 & 드롭으로 연결:
- Pet Friendly Toggle ← PetFriendlyToggle
- Public Data Toggle ← PublicDataToggle
- Subway Toggle ← SubwayToggle
- Bus Toggle ← BusToggle
- Alcohol Toggle ← AlcoholToggle
- Woopang Data Toggle ← WoopangDataToggle
- Select All Button ← SelectAllButton
- Deselect All Button ← DeselectAllButton
```

#### 5. 배치 순서 확인 (1분)
```
Hierarchy에서 다음 순서로 정렬:
1. PetFriendlyToggle
2. PublicDataToggle
3. SubwayToggle        ← 새로 추가
4. BusToggle           ← 새로 추가
5. AlcoholToggle       ← 새로 추가
6. WoopangDataToggle   ← 새로 추가
7. SelectAllButton     ← 새로 추가
8. DeselectAllButton   ← 새로 추가

(드래그하여 순서 변경 가능)
```

#### 6. 저장 및 씬 적용 (2분)
```
1. Prefab Mode 상단: "Save" 버튼 클릭
2. "<" 버튼 클릭 (씬으로 복귀)
3. Unity 메뉴: Tools > Woopang > Setup Filter Button Panel
4. 완료 메시지 확인
5. Ctrl+S (씬 저장)
```

## 🎨 최종 결과 미리보기

```
┌─────────────────────────────────┐ ← FilterButtonPanel
│  10px padding                   │   (250x600, 왼쪽 상단)
│                                 │
│  ☑ 애견동반          [60x60]    │ ← 토글 크기 2배
│  ☑ 공공데이터        [60x60]    │
│  ☑ 지하철            [60x60]    │ ← 새로 추가
│  ☑ 버스              [60x60]    │ ← 새로 추가
│  ☐ 주류판매          [60x60]    │ ← 새로 추가
│  ☑ 우팡데이터        [60x60]    │ ← 새로 추가
│                                 │
│  ┌───────────────────────────┐  │
│  │     전체 선택    [파랑]    │  │ ← 새로 추가
│  └───────────────────────────┘  │   [250x50]
│                                 │
│  ┌───────────────────────────┐  │
│  │     전체 해제    [회색]    │  │ ← 새로 추가
│  └───────────────────────────┘  │   [250x50]
│                                 │
└─────────────────────────────────┘

간격: 5px (VerticalLayoutGroup)
```

## 🧪 테스트 시나리오

### 일반 클릭 테스트
```
1. Play Mode 실행
2. "애견동반" 토글 클릭
   → 체크박스만 ON ↔ OFF 전환
   → 다른 토글들은 그대로 유지 ✅

3. "지하철" 토글 클릭
   → 독립적으로 ON/OFF 전환 ✅
```

### 길게 누르기 테스트
```
1. "버스" 토글을 0.8초 이상 누르고 있기
   → "버스"만 ON, 나머지 모두 OFF ✅
   → 콘솔: "[FilterManager] Long Press 감지: bus" ✅

2. "우팡데이터" 토글 길게 누르기
   → "우팡데이터"만 ON, 나머지 모두 OFF ✅
```

### 전체 선택/해제 테스트
```
1. "전체 해제" 버튼 클릭
   → 모든 토글이 OFF ✅
   → AR 오브젝트가 모두 사라짐 ✅

2. "전체 선택" 버튼 클릭
   → 모든 토글이 ON ✅
   → AR 오브젝트가 다시 나타남 ❓ (버그 확인!)
```

### AR 재활성화 버그 테스트
```
1. Console 창 열기 (Ctrl+Shift+C)
2. "우팡데이터" 토글 OFF
   → 콘솔: "[DataManager] 필터 적용 완료 - 표시: 0개, 숨김: 15개"
   → AR 큐브들이 사라짐 ✅

3. "우팡데이터" 토글 다시 ON
   → 콘솔: "[DataManager] 필터 적용 완료 - 표시: 15개, 숨김: 0개"
   → 콘솔: "[DataManager] placeId=123 '카페' - 비활성 → 활성"
   → AR 큐브들이 다시 나타나야 함 ❓

4. 만약 나타나지 않으면:
   - Console 로그 전체 복사
   - 스크린샷 촬영
   - 버그 리포트 작성
```

## 🐛 문제 해결

### "FilterManager를 찾을 수 없습니다"
```
→ FilterButtonPanel GameObject에 FilterManager.cs 컴포넌트 추가
   (Add Component > FilterManager)
```

### "프리팹에 X개의 UI 요소가 누락되어 있습니다"
```
→ 위 "2. 토글 4개 추가" 단계 다시 확인
→ 각 토글/버튼이 정확히 만들어졌는지 확인
```

### "Toggle 참조가 null입니다"
```
→ "4. FilterManager 연결" 단계 다시 수행
→ Inspector에서 모든 필드에 참조가 설정되었는지 확인
```

### AR 오브젝트가 재활성화되지 않음
```
→ Console 로그 확인:
   - "SetActive(true)" 호출되는가?
   - "비활성 → 활성" 전환 로그가 있는가?
   - spawnedObjects.Count가 0보다 큰가?

→ 로그 예시:
   [DataManager] ApplyFilters - woopangData=True, spawnedObjects 개수=15
   [DataManager] placeId=1 '카페A' - 비활성 → 활성
   [DataManager] placeId=2 '카페B' - 비활성 → 활성
   ...
   [DataManager] 필터 적용 완료 - 표시: 15개, 숨김: 0개
```

## 📚 참고 문서
- 상세 가이드: `FilterButtonPanel_완성가이드.md`
- 업데이트 요약: `FILTER_SYSTEM_UPDATE_SUMMARY.md`
- 코드 파일:
  - `Assets/Scripts/UI/FilterManager.cs`
  - `Assets/Scripts/Download/DataManager.cs`
  - `Assets/Scripts/Editor/WoopangSceneSetupHelper.cs`

## ✅ 완료 체크리스트

작업 완료 후 체크:
- [ ] 프리팹에 8개 UI 요소 모두 추가됨 (6 토글 + 2 버튼)
- [ ] FilterManager에 모든 참조 연결됨
- [ ] Tools > Woopang > Setup Filter Button Panel 실행 완료
- [ ] 일반 클릭 테스트 통과
- [ ] 길게 누르기 테스트 통과
- [ ] 전체 선택/해제 테스트 통과
- [ ] AR 재활성화 동작 확인 (또는 버그 리포트 작성)
- [ ] 씬 저장 완료 (Ctrl+S)

---

**예상 작업 시간**: 15-20분
**난이도**: ⭐⭐☆☆☆ (초급-중급)
